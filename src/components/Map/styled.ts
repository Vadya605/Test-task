import { styled } from "@mui/material";

export const mapContainerStyle = {
    width: '100%',
    height: '100%',
};

export const MapWrapper = styled('div')(() => ({
    width: '100%',
    height: '100%',
    position: 'relative'
}))